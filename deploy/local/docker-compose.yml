services:
  app:
    image: node:24
    working_dir: /workspace
    command: sh -c "npm install && npm run dev"
    ports:
      - "3000:3000"
    environment:
      HOST: 0.0.0.0
      PORT: 3000
    volumes:
      - ../..:/workspace
